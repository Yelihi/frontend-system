# Next.js mandatory rules

## Respect server and client boundaries

Keep secrets and server-only dependencies out of client bundles, and introduce client components only where browser interactivity requires them.

