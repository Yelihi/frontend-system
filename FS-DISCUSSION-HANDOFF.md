# FS 구조 보존 논의 — 재시작 인계 메모

기록일: 2026-09-19 (Asia/Seoul)

## 재개할 때 가장 먼저 읽을 내용

- 사용자는 Dune 철학을 frontend-system(FS)에 적용하는 논의안을 제시했다.
- 요청은 현재 FS 코드를 근거로 논의안을 보완하고, AI가 채울 내용과 사용자가 결정해야 할 내용을 자세히 안내하는 것이다.
- **구현 요청이나 승인된 revision이 아니다. 코드 수정·리팩터링을 시작하지 않는다.**
- 이전 답변이 너무 복잡했다. 사용자는 “해당 내용이 이해가 쉽진 않네요”라고 말했다.
- 다음 대화에서는 쉬운 말과 구체적인 예시 하나로 설명하고, 중요한 질문을 한 번에 하나씩 한다. 이전의 긴 보고서를 그대로 반복하거나 큰 입력 양식을 다시 요구하지 않는다.
- 사용자는 compact 후 Codex를 종료·재실행할 예정이며, 지금은 논의를 이어가기 위한 기억 보존을 요청했다.
- 아래 권고는 AI의 제안이다. 사용자가 선택하거나 승인한 것으로 취급하지 않는다.

## 사용자가 제시한 논의안의 핵심

핵심 명제: 올바른 선택은 찾기 쉽고 결정할 것이 적어야 하며, 잘못된 선택은 가능한 한 기계적 검사에서 실패해야 한다.

FS는 모든 프로젝트에 동일한 구조를 강요하지 않는다. 프로젝트별 중요한 보장을 정하고, 다음 세 가지를 함께 갖추는 방향이다.

1. 짧고 명확한 규칙.
2. 복사해서 구현할 수 있는 정상 사례.
3. 잘못된 변형을 실제로 거부하는 검사.

사용자가 제시한 다섯 원칙:

- 프로젝트별 정상 구현 경로를 쉽게 찾도록 한다.
- 금지된 의존성을 기계적으로 실패시킨다.
- 중요한 상태의 소유자와 변경 책임을 분명히 한다.
- 새 기능이 공유 루트의 분기와 충돌을 계속 늘리지 않도록 한다.
- 예외는 범위·이유·위험·검증·재검토 조건을 명시한다.

제안된 첫 실험은 소비 프로젝트 하나의 Next.js 서버/클라이언트 경계이다. FS 전용 범용 린터나 범용 계약 모델부터 만들자는 요청은 아니다.

## AI가 제안한 FS 목적 문장 — 아직 미승인

FS는 프로젝트의 도메인 보장과 유지 비용을 근거로 적합한 구조를 선택하도록 돕고, 합의된 중요한 계약이 이후 생성·구현·리팩터링에서도 보존되도록 한다.

성과는 규칙·폴더·검사 수보다 사용자 동작 보존, 회귀 방지, 반복 수정 비용으로 평가한다. 구조를 선택하는 판단과 선택한 구조를 유지하는 장치를 구분한다.

## 현재 저장소를 읽고 확인한 사항

아래는 이전 턴의 정적 코드 검토 결과다. 테스트를 실행해 검증한 결과가 아니며, 재개 시 관련 파일의 변경 여부를 확인한다.

| 근거 파일 | 확인 사항 |
| --- | --- |
| `src/domain/types.ts` | `EngineeringRule.mandatory`는 의무를 표현하지만 실행 검사와 연결되지 않는다. `ProjectConstraint`는 ID·설명·근거를 가진다. |
| `src/application/rules/rule-resolver.ts` | 규칙을 작업 컨텍스트에 제공한다. mandatory 자체가 자동 검사나 완료 차단을 구현하지 않는다. |
| `src/adapters/filesystem/project-discovery.ts` | 모든 package scripts를 목록화하지만 capabilities로 고르는 이름은 제한적이다. `check:boundaries` 등 `check:*`는 현재 자동 실행 대상에 포함되지 않는다. `build`, `lint`, `test:*` 등은 포함된다. |
| `src/application/run-capabilities.ts` | 검사 결과에 소스·revision 해시와 실행 중 변경 여부를 기록한다. `full`은 capability 선택 없이 실행했다는 뜻이며 필수 계약의 검사 누락 여부를 의미하지 않는다. |
| `src/application/workflow-store.ts` | revision 변경 시 승인을 무효화한다. 실행 완료 저장 시 현재 승인·소스·통과 기록을 대조한다. 최종 full check에는 test/e2e 계열 결과도 요구한다. 그러나 revision에서 요구한 필수 검사 ID 집합과 결과를 대조하지는 않는다. |
| `src/cli.ts` | `fs checks`는 하위 검사 실패를 결과로 출력하지만 그 실패를 CLI non-zero exit로 전달하는 처리가 보이지 않는다. CI gate로 바로 사용할 때 확인·보완이 필요하다. |
| `src/application/context/build-work-context.ts` | 구조 샘플은 경로와 검색 점수로 고른다. 승인된 정상 사례를 의미하지 않는다. |
| `skills/fs-refactor/SKILL.md` | 실행 기록을 통한 완료 경로를 명시한다. 일반 implement/verify 전체가 같은 완료 API를 반드시 거치는 것은 아니다. |
| `references/testing-and-transition.md` | 전체 리팩터링 시 테스트를 루트 `__test__`로 이동시키는 지침이 있다. 프로젝트별 적합한 배치를 존중하자는 새 제안과 정책 결정이 필요하다. |
| `references/knowledge-indexing.md` | 개념·조건부 판단·불확실한 자료를 구분한다. 외부 철학을 자동 mandatory로 만들지 않는 방향과 이미 부합한다. |

핵심 빈틈의 예시:

```text
revision에 경계 검사가 필요하다고 적음
→ 검사 스크립트를 삭제함
→ 남은 test/lint/build를 다시 실행하여 모두 통과함
→ 현재 완료 로직은 삭제된 필수 검사를 별도로 대조하지 못함
```

소스 해시는 검사 후 코드 변경을 잡는 데 유용하지만, 검사 자체를 제거한 새 소스에서 재실행한 경우의 누락 검증과는 다르다.

## 제안문에 대한 보완 권고 — 아직 미승인

- 의무, 규칙의 근거, 검사 방식, 강제 위치, 현재 실행 결과를 구분한다. 단일 enforcement 값에 모두 섞지 않는다.
- `mandatory`를 당장 교체하기보다 기존 의미를 유지하고 필요한 검사 연결부터 검토한다.
- Next.js의 `client → server/` 폴더 전체 금지는 부정확할 수 있다. 지원되는 Server Function import는 허용 경로다. 클라이언트 실행 그래프에 서버 전용 구현이 포함되는 문제로 계약을 구체화한다.
- 기존 `server-only` 및 build/lint가 충분한지 먼저 확인한다. ESLint·TypeScript·package exports가 동일한 검사 능력을 가진다고 가정하지 않는다.
- 단일 writer는 변경 함수 하나라는 뜻이 아니다. 서버 원본·캐시·폼 초안의 권위와 동기화·충돌 해결 책임을 구분한다.
- inspect는 정상 사례 후보를 발견하고, revise에서 적용 조건과 대표성을 판단한다. 계약 안의 모든 구현 변경에 새 승인을 요구하지 않는다.
- 공유 파일을 수정한다는 사실만으로 문제라고 판단하지 않는다. 실제 결합·충돌·회귀 비용을 확인한다.
- 자동 판정과 재현성은 별개다. 외부 환경에 의존하는 테스트가 항상 결정적인 것은 아니다.
- 테스트의 `__test__` 일괄 이동보다 기존 배치 보존을 기본으로 하고 필요할 때만 이동하는 방향을 제안했다. 사용자가 아직 결정하지 않았다.
- 기계 검사 전에 적용 계약과 검사 누락을 살펴야 한다. 검증 순서를 지나치게 고정된 파이프라인으로 만들지 않는다.
- 로컬 승인·해시는 실수 방지와 변경 추적 장치다. 파일 수정 권한을 가진 주체의 의도적 우회까지 막는 보안 경계라고 주장하지 않는다.

## 최소 실험과 변경 방향 — 아직 미승인

1. 실제 소비 프로젝트에서 계약 하나와 정상 사례 하나를 선정한다.
2. 기존 build/lint 등으로 위반을 탐지할 수 있는지 먼저 확인한다. 필요 없으면 FS 코드를 바꾸지 않는다.
3. 정상 사용은 통과하고 의도적 위반은 해당 경계 진단으로 실패하는지 확인한다. 무관한 빌드 실패는 탐지 성공으로 세지 않는다.
4. 직접 import, 프로젝트에서 쓰는 alias·재수출, 필요한 경우 동적 import를 확인하고 미지원 범위를 적는다.
5. FS의 발견·실행·결과 기록·완료 거부까지 별도로 확인한다.
6. 필수 검사 삭제, 미발견, 미실행, 실패, 오래된 소스·revision 결과로 완료할 수 없는지 확인한다.
7. 필요성이 확인되면 기존 revision/capability/check record/execution 구조에 필수 검사 ID 연결을 최소 추가한다. 별도 범용 계약 엔진은 보류한다.

필수 검사 연결은 승인 대상과 함께 해시되어야 한다. 승인된 revision 옆의 자유롭게 바뀌는 JSON만으로 연결을 관리하면 승인 의미가 약해진다. 저장 형식은 아직 결정하지 않았다.

기존 mandatory나 과거 실행 기록을 자동으로 새 강제 수준으로 승격하지 않는다. 실제 CI 강제를 주장하려면 CI 명령뿐 아니라 병합 필수 설정도 확인한다.

## 사용자가 아직 제공하거나 결정하지 않은 것

- 대표 소비 프로젝트 경로.
- AI가 반복해서 잘못 구현했던 실제 사례 하나.
- 반드시 보존해야 하는 사용자 동작과 이번 변경의 허용 범위.
- 기존 관행 중 의도한 팀 규칙과 우연히 생긴 구조의 구분.
- 허용할 예외, 검사 시간·유지 비용, 로컬/FS 완료/CI 중 강제 위치.
- Dune 원문 링크.

이 모든 항목을 한꺼번에 질문하지 않는다. 기술적인 사실·도구·설정은 AI가 조사한다. 사용자에게는 제품의 정답과 우선순위·허용 비용을 묻는다.

## 권장 재개 방식

먼저 “FS가 좋은 구현 예제를 보여 주고, 중요한 약속을 어기면 검사에서 잡히게 하자는 논의였다” 정도로 쉽게 설명한다.

사용자가 이해를 원하는지 실험을 시작하려는지 현재 요청에 맞추고, 구체적인 예시 하나로 논의한다. 실험을 원하면 대표 프로젝트 또는 실제 실패 사례 중 하나부터 질문한다. 이전의 기술 필드와 8개 쟁점 표부터 다시 설명하지 않는다.

## 작업 상태와 보존할 기존 변경

논의 검토 과정에서는 구현·테스트·revision을 수정하지 않았고, 승인·커밋도 하지 않았다. 이 인계 메모만 재시작 대비를 위해 새로 작성했다.

기록 당시 이미 존재하던 사용자 작업:

```text
 M README.md
 M knowledge/README.md
 M skills/fs-knowledge-add/SKILL.md
 M skills/fs-knowledge-sync/SKILL.md
?? references/linked-knowledge.md
```

이 변경을 이번 논의의 구현 결과로 취급하거나 되돌리지 않는다.

## 확인한 공식 자료

- Next.js 서버/클라이언트 경계 및 server-only: https://nextjs.org/docs/app/getting-started/server-and-client-components
- Next.js Server Function import: https://nextjs.org/docs/app/api-reference/directives/use-server
- Node.js package exports의 범위: https://nodejs.org/api/packages.html#package-entry-points
- GitHub 필수 상태 검사: https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches

Dune 원문은 확인하지 않았다. 출처를 추정해서 철학의 원저자 주장으로 단정하지 않는다.
